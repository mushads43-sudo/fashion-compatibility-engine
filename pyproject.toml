[project]
name = "fashion-compatibility-engine"
version = "1.0.0"
description = "Visual similarity and outfit compatibility engine for fashion products."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
